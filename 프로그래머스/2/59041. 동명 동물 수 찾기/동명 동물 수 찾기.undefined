SELECT
    NAME,
    COUNT(*) AS 'COUNT'
FROM ANIMAL_INS
WHERE NAME IN
    (SELECT NAME FROM ANIMAL_INS
    GROUP BY NAME
    HAVING COUNT(*) >= 2) AND
    NAME IS NOT NULL
GROUP BY NAME
ORDER BY NAME